# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini

# Netlify
.netlify/

# Node (if tooling is added later)
node_modules/

# Claude Code local machine settings (per-machine, not for the repo)
.claude/settings.local.json

# Raw media library (dyno sheets, videos, competitor comparisons, ad source files).
# Kept local + out of the published site/. Only curated images under site/images/ ship.
assets-source/

# Confidential Magnuson price-file extract — carries dealer/jobber cost tiers
# (margin data). Local internal reference only; never served, never committed.
data/magnuson-catalog.json
app/www/

# Python bytecode (last30days digest tooling)
__pycache__/
*.pyc
