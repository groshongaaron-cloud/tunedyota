---
name: lead-tracking-program
description: "Multi-channel lead tracker program — Core built (not yet deployed), Twilio+Gmail+Meta adapters sequenced"
metadata: 
  node_type: memory
  type: project
  originSessionId: c640a97c-46ce-420c-a71d-296eebb40609
---

Program to track incoming clients from 5 channels (Google email, Facebook, Instagram, text, phone) → future clients, built INTO the Tuned Yota app (installer console). Decomposed into a shared **Core** + per-channel **adapters**.

**Decisions (2026-07-14):** capture = **automatic ingestion** (owner's choice); build order = Core → Email(Gmail) → SMS/Calls(Twilio) → FB/IG(Meta). **Twilio APPROVED** as the SMS+call pillar (~$3–8/mo typical; $40 budget is generous). **Port 612-406-7117 from T-Mobile → Twilio** (a port is required to auto-capture the number's *texts*; keeps the number identical; eSIM is fine; Claude owns the port mechanics, owner does the ~5-min carrier auth: T-Mobile transfer PIN + account# + LOA). Calls forward to the 3 installers' personal cells (numbers held by owner / to be placed in **secure Netlify config, NOT git/memory** at adapter build time). Google Voice was rejected — **no public API**, can't automate. Comments (FB/IG) = phase 2.

**Core — SHIPPED LIVE + verified (2026-07-14, master @ a6473df):** spec `docs/superpowers/specs/2026-07-14-lead-tracker-core-design.md`, plan `docs/superpowers/plans/2026-07-14-lead-tracker-core.md`. Adds a pipeline layer on the existing **Priority List** Airtable table (New→Contacted→Following up→Booked→Not now). Files: `netlify/functions/lib/leads.js` (pure logic: normalizeChannel/normalizePhone/normalizeEmail/toLeadView/scopeLeads/processLeadIngest[cross-channel dedupe]/applyLeadUpdate/dueLeads) + endpoints `lead-ingest.js` (installer token OR `INTERNAL_TASK_SECRET` for adapters), `leads-list.js` (scoped: installer=own markets, admin=all+Unassigned+summary), `lead-update.js` (setStage/logContact/setFollowup/reassign/convert→Booking), scheduled `lead-followups.js` (`0 13 * * *`, web-push due leads per installer). UI = a **`Jobs · Leads` toggle** in `site/installer.html` (pipeline list, +Log a lead, call/text/email deep-links, stage/follow-up/convert actions, due badge). 610 tests green (29 new incl. a Playwright leads-browser test). Built subagent-driven (fresh worktree per agent, cherry-picked to master).

**Owner setup DONE 2026-07-14:** the **6 Priority List columns** (`Stage`/`Channel` single-selects, `Next Follow-up`/`Last Contact` dates, `Activity Log` long text, `Converted Booking` single line text) were created via the owner's ephemeral schema token (clipboard workflow, [[airtable-metadata-api]]) — write-tested, then live-verified: a `lead-ingest` POST (task-secret adapter path) on prod persisted Stage=New/Channel=sms/Installer=cody/Activity-Log-timestamp correctly (record deleted after). Deployed via `git push` master; `leads-list` fail-closed 401 confirmed live. `INTERNAL_TASK_SECRET` already set (gates the adapter ingest path). No open items on the Core. **Reminder for adapters:** each channel adapter (Gmail/Twilio/Meta) POSTs the normalized shape to `/lead-ingest` with header `x-ty-task: $INTERNAL_TASK_SECRET`.

**Adapter #2 — OTT Email Loop (Gmail): INGEST HALF ACTIVATED + VERIFIED LIVE END-TO-END (2026-07-15).** Spec `docs/superpowers/specs/2026-07-15-ott-email-lead-loop-design.md`, plan `docs/superpowers/plans/2026-07-15-ott-email-lead-loop.md`. Purpose: OTT emails `info@tunedyota.com` Facebook-Ads leads (subject **"A New Lead From Facebook Ads"**); ingest → tracked lead tagged **`channel=ott-national`** (owner's chosen tag; capture Gmail thread/message-id/reply-to) → when the tune is **marked Completed at close-out**, **auto-reply in the original email thread** confirming completion + stamp `OTT Reply Sent` (idempotent). Files: `lib/gmail.js` (OAuth2 refresh-token REST client, no new deps), `lib/ott-email.js` (`parseOttLeadEmail`), scheduled `gmail-lead-poll.js` (*/10, **60-day lookback `newer_than:60d`** so first activation backfills; parse→`/lead-ingest` via `x-ty-task`→label `ty-ingested`), scheduled `ott-reply-sweep.js` (*/15, Completed booking→Gmail reply→stamp). Core extended: `ott-national` channel + `processLeadIngest` persists email refs. 623 tests green; both fns verified live inert (poll `{skipped:no-gmail-config}`, sweep `{replied:0,scanned:12}`). **ACTIVATION GATES (owner-side, none blocking):** (1) **Airtable — DONE 2026-07-15:** 4 Priority List cols `Email Thread`/`Email Message-Id`/`Reply-To` (single line) + `OTT Reply Sent` (date) created via schema token + write-verified; `ott-national` Channel option auto-created via typecast write (the metadata PATCH to add a select choice 422'd — Airtable field-option PATCH is finicky — but `typecast:true` on the data write auto-creates the option, so no PATCH needed). (2) **Google OAuth — DONE 2026-07-15:** owner created a **Desktop** OAuth client (Internal consent) for `info@tunedyota.com` in Google Cloud (Gmail API enabled); captured `GMAIL_CLIENT_ID`/`GMAIL_CLIENT_SECRET`/`GMAIL_REFRESH_TOKEN` (scopes `gmail.modify`+`gmail.send`) via a local 127.0.0.1 loopback catcher → `netlify env:set`. **Refresh token is Internal-app = non-expiring** (why Internal matters — External/Testing tokens die in 7 days). Owner should keep the `ty-gmail-desktop` OAuth client; if the token ever breaks, re-run the loopback consent. (3) **Real sample — DONE 2026-07-15:** pulled a real lead via the live token. **The email DOES carry full customer data** (open Q resolved) — forwarded by `info@overlandtailor.com`, subject `Fw: A New Lead From Facebook Ads - <Name>`, labeled body `Name/Email/Phone(+1XXX | (XXX)…)/City/State/Vehicle Year|Make|Model/Engine Size/Transmission/Engine modifications`. Parser finalized (@ 9e4fca6): combines split vehicle fields, first phone format, location+engine→Goals; reply target = From (`info@overlandtailor.com`). Fixture sanitized (no PII). **VERIFIED LIVE END-TO-END:** sent a synthetic test email → live poll `{ingested:1}` → lead created correct (Channel ott-national, Installer cody from Omaha routing, phone/vehicle/goals/thread-refs all right) → deleted + email trashed. **Ingest half is fully live.** Note: existing real OTT emails are ~94d old (outside the 60d window) so nothing backfilled; future leads flow automatically. **Still unproven-live: the completion-reply half** (`ott-reply-sweep` Gmail send) — unit-tested + send capability proven (used it to send the test email), but no real completed OTT booking has exercised it yet; will confirm on the first OTT lead that gets converted→closed-out, or test with a controlled booking. **Adapter #3 (Twilio SMS+Voice) BUILT + unit-tested on master** (dependency-free webhooks: `lib/twilio.js` + `twilio-sms`/`twilio-voice`/`twilio-voice-transcription`; signature-validated HMAC-SHA1 fail-closed; POSTs `/lead-ingest` via `x-ty-task`). INERT pending activation — see `docs/operations/twilio-adapter-activation.md` (owner: set `TWILIO_AUTH_TOKEN` + `TWILIO_FORWARD_NUMBERS` env vars + wire a temp Twilio number to the two webhooks; real-line cutover deferred to port time). Adapter #4 Meta still pending.

Related: [[funnel-roadmap-and-lead-setup]], [[certificate-v2-dashboard-program]], [[event-reminders-automation]], [[installer-console-access]], [[tunedyota-app-program]], [[airtable-metadata-api]], [[prefer-automation-over-handoffs]], [[monthly-ott-calibration-report]], [[email-sending-infra]].
