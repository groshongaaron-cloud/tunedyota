---
name: cloud-routines
description: Three scheduled cloud agents (routines) monitor tunedyota.com and post to Slack; manage at claude.ai/code/routines
metadata: 
  node_type: memory
  type: project
  originSessionId: e75d74d1-a76c-483c-83d5-58cb118bc0c4
---

Set up 2026-06-18 via `/schedule`. Three Anthropic-cloud routines run against the connected repo `github.com/groshongaaron-cloud/tunedyota` (model `claude-sonnet-4-6`). Manage/disable/delete at https://claude.ai/code/routines (the RemoteTrigger API can list/get/create/update/run but CANNOT delete).

- **TunedYota — Deploy & Uptime Watch** (`trig_01QLvJCxV1PwoyEMKhRfWKRW`) — daily 13:00 UTC (8am CDT). Read-only: curls 9 live URLs for HTTP 200 + checks sitemap (19 locs), Event/`tf-proof`/`og:title` markup, the `availability` function, and homepage JSON-LD parse. No repo changes.
- **TunedYota — Event Schedule Freshness** (`trig_015HVaAPwEqh8UbBkGXfW9Tc`) — Mondays 13:00 UTC. Flips genuinely past-dated events in `netlify/functions/lib/events-data.js` to `active:false`, runs `npm run build:seo` + `npm test`, opens a PR. No-ops (and still posts) when all events are upcoming.
- **TunedYota — SEO & Booking Smoke Test** (`trig_01CdqTjrADVdTERzwB3g8DFe`) — Thursdays 13:00 UTC. Runs `npm test`, validates live schema/sitemap/OG + the `/services` breadcrumb stays gone, hits `availability`; opens a PR if only SEO drift tests fail. (Booking-backend health is thus checked daily by the uptime watch and weekly here — no separate booking routine.)
- ~~**TunedYota — Monthly GSC Review Reminder** (`trig_014SdCp5ifkJ2hPwmnwgt8gj`)~~ — **DELETED 2026-07-01.** Replaced by the local search-visibility measurement engine (below). So only 3 cloud routines remain (uptime / event-freshness / SEO smoke).

**Search-visibility measurement runs LOCALLY now, not as a cloud routine.** A cloud `search-visibility-tracker` routine was built + deleted 2026-07-01 (cloud routines can't `git push`/PR — read-only GitHub integration — and `RemoteTrigger` echoes embedded secrets into the transcript). Replaced by a **Windows Task Scheduler** job `TunedYota Search Visibility` (monthly, 1st 08:00) → `~/.tunedyota/run-measure.cmd` → `node scripts/measure/run-local.mjs`, secrets in `~/.tunedyota/measure.config.json`. Pulls GSC + Perplexity → snapshot in `docs/seo/measurements/` → Slack report → local `git push`. See [[search-ai-visibility-program]] Phase 3 + `docs/seo/measurement-local.md`.

**🚨 ROOT CAUSE (2026-07-02) — cloud routine sandbox BLOCKED all outbound egress; RESOLVED same day by setting Network access = Full.** Reading the run outputs in the claude.ai routines UI showed EVERY run (manual + scheduled, back to creation) ended **"Monitor run result: BLOCKED"**: the remote-env egress proxy returned `403 connect_rejected` on every `CONNECT` to `tunedyota.com:443` AND `hooks.slack.com`. So NO check ever got data — runs showed "Completed" (misleading) and only an Anthropic **PushNotification** got through (owner never noticed). The earlier "stale/dead webhook" theory was a RED HERRING — routines never reached Slack to hit any webhook; blocked at the proxy first. **FIX APPLIED:** claude.ai/code → composer env chip **"Default"** (⚙ gear) → **Update cloud environment → Network access** was **"Trusted"** (packages only) → changed to **"Full"** (unrestricted outbound) + Save. Options there: None / Trusted / Full / Custom(allowlist). Chose Full because the 3 monitors need site + `/notify` relay + `npm install` + `gh` PRs — a `tunedyota.com`-only allowlist would only fix the uptime watch. Applies to NEW sessions (this env = `env_017N92hSZaxt84mH1KRkFjTA`, the one all routines use). **VERIFIED 2026-07-02 — ALL 3 monitors re-run manually, all now work end-to-end (real data + Slack via /notify relay to Tunedyota Alerts):** (1) Deploy & Uptime Watch → 5 checks ran (HTTP 200×9, booking healthy, JSON-LD ok); (2) SEO & Booking Smoke Test → 219/219 tests pass, live SEO checks pass incl. new sitemap 29==29 drift check, no PR needed; (3) Event Schedule Freshness → found 4 genuinely-past-but-still-active events (twin cities Jun20, cedar rapids Jun27, des moines/omaha Jun28), flipped to active:false, ran build:seo + test (219 pass), **pushed a branch + opened PR #1** (github.com/groshongaaron-cloud/tunedyota/pull/1), Slack ok. **⚠ IMPORTANT COROLLARY: cloud routines CAN git-push + open PRs after all** — the old "read-only GitHub integration, can't push" belief (which drove moving the measurement engine local) was ALSO just the egress block (github.com unreachable under "Trusted"); under "Full" the gh CLI push+PR works. Measurement engine stays local (works fine) but that constraint is now known false. **PR #1 MERGED 2026-07-02** (owner approved) → master `a2ecc13`; the 4 past events are deactivated live (verified: Omaha/Twin Cities/Cedar Rapids/Des Moines availability now `hasEvent:false`, future Fargo/Madison still `true`); merged locally via git + branch deleted. **`gh` CLI now INSTALLED 2026-07-02** (winget → GitHub.cli v2.95.0 at `C:\Program Files\GitHub CLI\gh.exe`, on PATH) but **NOT yet authenticated** (no `hosts.yml`, no GH_TOKEN) → owner must run `gh auth login` once in a REAL terminal (interactive browser flow won't complete via the chat `!` prefix). Until then, PR ops still need the manual git merge dance. To revert/tighten later: same dialog, switch to Custom + allowlist. **Only-once-working monitor immediately caught real drift:** sitemap has **29** `<loc>` (repo + live match) but both the uptime + SEO-smoke routines hard-coded "exactly 19" → FALSE FAIL. Fixed both prompts 2026-07-02 to a SELF-MAINTAINING check (compare live `grep -c '<loc>'` vs repo `site/sitemap.xml` count; must be EQUAL = deploy-drift detector, no magic number).

**Notifications: routines now post via a Netlify Slack RELAY, not an embedded webhook (RESOLVED 2026-07-02, proxy option chosen) — but see the egress BLOCK above; the relay is only reachable once the sandbox allows `tunedyota.com` egress.** OAuth/claude.ai connectors don't reach headless cron, so notifications go over HTTP. The 3 recurring monitors now `curl` **`https://tunedyota.com/.netlify/functions/notify`** with header `x-ty-notify: <NOTIFY_TOKEN>` and body `{"text": "..."}`; `netlify/functions/notify.js` forwards to Slack via `lib/alert.js` using the server-side `SLACK_WEBHOOK_URL` env var. **The raw Slack webhook now lives ONLY in the Netlify env var** — rotating Slack = update that ONE env var, never touch a routine prompt again (and never re-leak the webhook through RemoteTrigger's prompt echo). The relay is gated by `NOTIFY_TOKEN` (a self-minted, low-value relay key set in Netlify env; worst case on leak = Slack-channel spam → rotate the one env var + re-update the 3 prompts). Tested end-to-end 2026-07-02 (all 4 auth/validation paths + a live relayed message + a real routine `run`). Code: `netlify/functions/notify.js` + `tests/notify.test.js` (8 tests), commit `942b540`.

**Prior STALE-webhook issue (2026-06-29 → 2026-07-02): FIXED by the relay above.** History: the 2026-06-29 rotation wasn't propagated to the routines, so their embedded webhook was DEAD (`POST → HTTP 404 no_service`) and posts silently failed. Confirmed 2026-07-02 that the **Netlify `SLACK_WEBHOOK_URL` is LIVE** (`env:get` returns it; test-post → `HTTP 200`), so `lib/alert.js` app-failure alerts were fine all along — only the routines were broken, now fixed. ⚠ **Note a webhook discrepancy to reconcile:** the Netlify env webhook (`.../B0BE3A1NQJY/...`), the local-config one in `~/.tunedyota/measure.config.json` (`slackWebhookUrl`), and the old routine-embedded one (`.../B0BCJL4Q74Y/...`) are three DIFFERENT URLs; the first two both return 200 (possibly different channels). The relay standardizes routines on the Netlify env var — worth confirming both live webhooks point to the intended channel. **Routine count:** there are **4 recurring routines** total — the 3 Slack monitors above + **`TunedYota — community radar`** (`trig_01M8hvsdR852yph2Yg1kpCh9`, daily 11:00 UTC, WebSearch leads/engagement digest; writes to the routines UI, no Slack, so untouched). Constraint still true: `RemoteTrigger update/get/list` echoes the full prompt into the transcript — which is exactly why the raw webhook must stay out of prompts (the relay achieves that). See [[pending-secret-rotation]].

To change schedules/prompts, use `/schedule` → Update (or RemoteTrigger `update`). Cron is UTC; CDT = UTC−5 (summer).
