---
name: amsoil-garage-program
description: Tuned Yota AMSOIL dealer storefront /amsoil-garage — Phase 1 LIVE 2026-07-10; referral ordering + PC/Dealer conversion funnel; price agent blocked by Cloudflare
metadata: 
  node_type: memory
  type: project
  originSessionId: a9d96be2-c5e9-42c3-ad0e-b88c7070495c
---

Tuned Yota is an authorized AMSOIL dealer (dealer referral / ZO number **30713116**). This program turns the site (and later the [[tunedyota-app-program]] Capacitor app) into a branded AMSOIL storefront + service-intelligence tool for the supported Toyota/Lexus lineup.

**Business model = conversion funnel, not just a store.** Every visitor can buy AMSOIL at retail through a Tuned-Yota-branded experience; the order completes on amsoil.com with `?zo=30713116` attached (30-day device attribution) → commission. The jackpot is converting visitors to **Preferred Customer or Dealer** — once enrolled under the ZO they're permanently associated with Tuned Yota and buy under it forever, hands-off. **Hard constraint:** AMSOIL exposes no ordering/payment API; all checkout happens on amsoil.com. We own the experience; AMSOIL owns the cart (zero inventory/payment liability).

**Phase 1 LIVE 2026-07-10** at https://tunedyota.com/amsoil-garage (master @ d354594; built subagent-driven on branch `amsoil-garage`, 380 tests). Hybrid look ("Direction C": TY warm brand frame, AMSOIL-authentic product cards). The page's catalog + Preferred-Customer + Dealer CTAs earn from day one; per-vehicle fluid garages are hidden until each generation is owner-verified.

**Discoverability pass SHIPPED 2026-07-10 (master @ ad8c8c8).** The storefront was live but an SEO orphan — zero internal links, absent from nav/footer + `llms.txt`, no product/FAQ schema. Fixed: AMSOIL added to header nav + footer across all 38 pages (path-aware codemod — `/amsoil-garage` on pretty-URL pages, `.html` elsewhere; inserted after the Supercharger link); dedicated "AMSOIL for your build" section + card on the homepage; `site/llms.txt` got a Key-pages entry, a dedicated "AMSOIL synthetic fluids (Authorized AMSOIL Dealer)" section, and 3 AMSOIL FAQ answers; `site/amsoil-garage.html` got Store + OfferCatalog + BreadcrumbList + FAQPage JSON-LD (category-level offers, **no hardcoded prices** to avoid drift vs the weekly price-sync) plus a keyword-targeted "Why AMSOIL for a tuned Toyota/Lexus" content block + on-page FAQ (Tier 2 shell). 385 tests green, JSON-LD 4/4 valid, all verified live. NOTE: Netlify serves pretty-URLs with single quotes (`href='/amsoil-garage'`) — account for that when grepping served HTML. Owner-side GBP "AMSOIL Dealer" category still pending.

**Per-vehicle AMSOIL landing pages SHIPPED LIVE 2026-07-10 (master @ 01afa35).** 13 platform pages (`site/amsoil-<make>-<model>.html`, e.g. `/amsoil-toyota-tundra`) generated by **`scripts/build-amsoil-pages.mjs`** (exports `buildAmsoilPages()` + `AMSOIL_PAGE_FILES`; wired into `build:seo` main() so one command regenerates them, **byte-stable/idempotent**; also `npm run build:amsoil`). Registered in `seo-data.mjs` HEAD_PAGES → OG/business/sitemap coverage. Each page: per-generation cards with the correct AMSOIL product + Ea filter code + gear lube + ATF, referral-attached order links, Store/OfferCatalog/FAQPage/Breadcrumb JSON-LD, cross-links to the vehicle's OTT page. **INTEGRITY RULE (baked in + test-guarded):** product family/viscosity/filter code are AMSOIL-API-authoritative → always shown; fluid **capacities + drain intervals are Toyota-OEM drafts → rendered ONLY when a generation is `verified:true`** (all currently false, so no capacity chips render — they light up automatically as installers sign off). So the `verified:true` flip now enriches BOTH the garage picker AND these SEO pages. Internal linking: "AMSOIL by vehicle" hub added to `amsoil-garage.html` + "AMSOIL fluids" link added to all 13 OTT pages + per-vehicle links in `llms.txt`. Also fixed `build-state-pages.mjs` NAV/FOOTER to include the AMSOIL link (was stale → a state-page regen would've dropped it). Guard: `tests/amsoil-vehicle-pages.test.js` (count, schema, capacity-withheld invariant, referral on order links) — **it must NOT call `buildAmsoilPages()`** (that writes raw pages and strips the OG/#business stub build-seo injects → breaks seo.test.js under one `node --test`); read committed output only. 389 tests green.

Key files (all under repo):
- `site/amsoil-referral.js` — the ONLY home of the ZO number (`30713116`, public, not secret — appears in URLs); `amsoilUrl(path,zo)` attaches `?zo=`/`&zo=`.
- `site/amsoil-garage.json` — data: `products` (by SKU) + `vehicles` (make→model→generation `{y,e,verified,systems,bundle}`). The `verified:false` flag GATES the live picker. Guarded by `tests/amsoil-garage-data.test.js` (SKU integrity + subset-of-lineup vs `vehicle-pricing.js` + capacity/unit types + unique bundles).
- `site/amsoil-garage-render.js` — pure resolve/bundle logic. `site/amsoil-garage.html` — the page (registered in `scripts/lib/seo-data.mjs` HEAD_PAGES).
- `scripts/amsoil/price-sync.mjs` + `lib/price-parse.mjs` + `lib/sync.mjs` — weekly retail-price agent (JSON-LD parser + ±40% guardrail, Slack via `/notify`, `--commit` git push).

**Price agent LIVE (headless browser beats Cloudflare).** amsoil.com is behind Cloudflare (plain server `fetch` → 403), so the agent uses **Playwright headless Chromium** (`scripts/amsoil/lib/browser-fetch.mjs`): a FRESH browser context per product resets the CF cookie (a reused session gets fingerprinted+blocked); stability flags (`--disable-dev-shm-usage/--disable-gpu/--disable-logging`) stop the network-service crash + quiet the log; each product is isolated in try/catch (errors→HELD, browser relaunches if it dies). All 9 products now carry **real `/p/` URLs + live retail prices** (SS oils $17.99, SEVERE GEAR $24.59/$26.69, Multi-Vehicle ATF $24.19, Ea filters $10.60; the 3 Ea filters share one selector page `/p/amsoil-oil-filter-eaoilfilt/?code=`). **Scheduled**: Windows Task Scheduler job "TunedYota AMSOIL Price Sync" (Sun 3:15 AM) → wrapper `C:\Users\grosh\.tunedyota\run-amsoil-price-sync.cmd` (SAFE: only runs on a CLEAN master, else skips + logs — this repo is actively branch-switched) → `node scripts/amsoil/price-sync.mjs --commit` (guardrailed commit+push). Uses `TY_NOTIFY_TOKEN` for Slack. NOTE: manually triggering the task from an active interactive session kills it with 0xC000013A (console teardown) — test the runner detached instead; the scheduler-fired run is unaffected. Playwright is a devDependency; the host needs `npx playwright install chromium` (already done on grosh's machine).

**OPEN — fluid data verification:** full DRAFT dataset for all 13 platforms / 38 generations lives on branch `amsoil-fluid-data` (all `verified:false`) + `docs/amsoil/fluid-data-verification.md` (per-platform sign-off tables). Owner/installers must verify capacities + intervals + real product URLs/prices, then flip `verified:true` per generation to light each garage live. **VERIFY HELPER SHIPPED 2026-07-10 (master @ ce74d93):** `node scripts/amsoil/verify-platform.mjs list|review|set-cap|confirm|unverify` — review-then-confirm CLI (pure logic in `scripts/amsoil/lib/verify.mjs`, IO in the CLI; writer matches price-sync's `JSON.stringify(,,2)` so a confirm is a ~1-line diff). `review "<Make Model>"` prints the draft specs to check; `set-cap "<Make Model>" "<year>" "<System>" <n>` corrects a capacity by command (no hand-editing JSON); `confirm "<Make Model>" [--year ..] [--build]` flips verified:true (publishes capacities+intervals to garage + vehicle page). Workflow documented in `docs/amsoil/fluid-data-verification.md`; guarded by `tests/amsoil-verify.test.js` (clone-only, no disk writes). Known weak spots: Tundra 2007-2021/2000-2009 transfer-case service (structural gap), 4Runner 2005-2009 dual-engine capacity, LS460 oil capacity, GX460 post-2019 viscosity.

**AMSOIL guide data pulled + verification doc pre-filled (2026-07-10, master @ eb7a6d0).** Reverse-engineered AMSOIL's Auto & Light Truck **fitment API** (no auth, reachable from a Playwright browser context after loading `/lookup/auto-and-light-truck/`): base `https://api-1.amsoil.com/api/`; cascade `Fitment/GetYears?lookupCode=autoandlighttruck` → `GetMakes?lookupCode=..&year=YYYY` (Toyota id 59) → `GetModels?..&equipmentMakeId=ID` → `GetEngines` → **`GetRecommendations?equipmentUnitId=U`** (the payload). Covered 27/28 generations. **The guide gives authoritative product family + viscosity + Ea filter code + drain interval, but NOT capacities/torque** — so capacities stay Toyota-spec DRAFTS needing installer confirmation (the owner's "pull authoritative capacities" hope is only partly met). `docs/amsoil/fluid-data-verification.md` now has an "AMSOIL Guide" column per platform. **Corrections APPLIED to `amsoil-garage.json` (master @ 3191734, 2026-07-10):** added 7 real products w/ live prices — per-engine Ea filters EA15K09 ($17.20 T24A-FTS) / EA15K04 ($19.45 3UR 5.7) / EA15K02 ($20.45 2GR V6) / EA15K49 ($35.80 1GR/1UR) / EA15K51 ($19.55 older), SEVERE GEAR **75W-85 SVL-QT** ($24.59), **80W-90 AGLPK-QT** ($21.39); rewrote every generation → correct per-engine filter, **0W-20** on 5.7L/4.0L fleet (RAV4 3.5L=5W-30), diffs→75W-85 (FJ07-09 & GX470→80W-90), Tundra 2022+ transfer-case ATF row added. Filters use selector page `/p/amsoil-oil-filter-eaoilfilt/?code=<CODE>-EA` (per-code prices DO vary — earlier "$10.60 flat" was a bad read). All 36 generations STILL `verified:false`; capacities unchanged (still Toyota-spec drafts). **Remaining gate = capacity confirmation → flip `verified:true` per platform to go live.** Owner-verify flag: 2025 4Runner had no Ea filter in AMSOIL's DB (EA15K09 applied by engine match — confirm fitment before that gen goes live).

**GARAGE LANDING ENHANCEMENTS SHIPPED 2026-07-13 (master @ ebc9cb2, part of Certificate v2 — see [[tuned-yota-master-certificate]] / [[certificate-v2-dashboard-program]]):** the Certificate of Calibration now prints a per-vehicle AMSOIL reference page whose **QR deep-links to `/amsoil-garage?make=&model=&year=`**, so `site/amsoil-garage.html` gained: **deep-link pre-fill** (reads `?make=&model=&year=` and pre-selects the picker — NOTE: `year` doesn't yet match the year-dropdown's label format, minor polish TODO; make+model prefill works), a **device-local "My Garage"** (localStorage `ty_amsoil_garage`, multi-vehicle chips — a precursor to P2's account-backed garage), and a **"Search all AMSOIL products"** box that opens amsoil.com search via `amsoilUrl()` with `?zo=`. **Prefill + gate finalized 2026-07-13 (master @ 4e3598a):** the `?year=` now matches range labels via `AmsoilGarage.inRange`, AND — importantly — **the `/amsoil-garage` landing's `verified` gate was RELAXED** (`resolveVehicle` opt-in `includeUnverified` + `yearsFor`/`sync` no longer filter `verified`) so the HUB now shows fluids for ALL generations, matching the certificate. **⚠ INTEGRITY-MODEL CHANGE:** the "capacities/intervals shown only when `verified:true`" rule from the note above now applies ONLY to the **13 per-vehicle SEO pages** (`build-amsoil-pages.mjs`, still test-guarded) — the interactive garage hub no longer gates. Each product in `amsoil-garage.json` now carries an official **`stockNo`** (ASMQT/ASLQT/ALMQT/SVLQT/SVGQT/SVOQT/AGLQT/ATLQT + the Ea filter codes) — owner to verify vs amsoil.com. `amsoil-fluids.js` (`netlify/functions/lib/`) resolves catalog→cert.

**V8 OIL-CAPACITY CORRECTION + CAPACITY-IS-UNGATED FINDING (2026-07-14, master @ ff465d0).** A factory-spec cross-check of draft engine-oil capacities found a unit error: several V8s held the **liter** value in the quart field. Corrected (owner-approved, still `verified:false`): **5.7L 3UR-FE 7.4→7.9 qt** (Tundra 07-21, Sequoia 08-22, LC 08-21, LX570) and **4.7L 2UZ-FE 6.1→6.4 qt** (Tundra 00-09, Sequoia 01-09, LC 06-07, GX 05-09). **KEY GOTCHA:** the `verified:false` gate only withholds capacities on the **SEO pages + garage** — the **follow-up email (`amsoil-email.js`) and the certificate render capacity UNGATED** (straight from `resolveFluids`), so a wrong draft capacity reaches customers even before verification (2 already-sent Tundra follow-ups on 2026-07-14 showed the old 7.4). So capacities must be kept correct regardless of the flag. Remaining engine-oil rows still need **installer confirmation** ("what do we actually pour") — list + punch-list in `docs/amsoil/fluid-data-verification.md` correction log (2GR variants, 4.6L 1UR inconsistency 6.4 vs 6.9, 2.4T ≈5.0, LS460, 2.7L, 4Runner 05-09 dual-engine). Once installers confirm, `set-cap` + `confirm --build` lights up the oil-change kit (blocked on this) + garage. The **oil-change kit bundles** feature (strategy item #7) is DEFERRED pending these verified capacities.

**North-star Phases 2–4** (designed-around, not built): P2 client accounts + persistent multi-vehicle My Garage (device-local precursor now live, see above); P3 mileage tracking + service-due reminders (email on web, push in app) — consumes the catalog's `tunedInterval`; P4 app parity + PC-share growth loop + launch email to the 4-year client list.

Spec: `docs/superpowers/specs/2026-07-09-amsoil-garage-design.md` (§5.1 = Cloudflare blocker). Plan: `docs/superpowers/plans/2026-07-09-amsoil-garage-phase1.md`.

---

**SESSION 2026-07-13 (late) — official pricing, real product images, attribution deep-link + OPEN ITEMS.**

- **Official U.S. pricing sheet applied + SHIPPED (master @ 98d6660).** Owner's authoritative AMSOIL "U.S. Pricing.csv" reviewed against all 13 catalog SKUs (retail = the sheet's **Online/Catalog** column, = amsoil.com online price). 10 already matched; 2 price drops applied (**80W-90 AGL $21.39→$19.49**, **ATF $24.19→$21.19**); superseded the price list in the price-agent note above. 2 **stock-number corrections** (owner-approved): the "Multi-Vehicle ATF" was mis-coded `ATLQT` (that's Fuel-Efficient) → corrected to **`ATFQT`**; the 75W-85 SEVERE GEAR `SVLQT` (non-existent quart bottle) → **`SVLPK-EA`** (real Quart Easy-Pack). Also **garage hero copy** cleaned per owner ("exact"/"Toyota or Lexus"/"severe" removed; now "The AMSOIL synthetic fluids for your vehicle — capacities and service intervals…"). NOTE: 75W-85/75W-140/80W-90 SEVERE GEAR are Easy-Packs only (no quart bottle).

- **Real AMSOIL product images self-hosted (BUILT, tested 548 green, ⚠ PENDING owner ship-approval).** All 13 catalog product images were **missing** (only the logo existed). Pulled 11 real product shots from the AMSOIL **dealer digital library** (`dz.amsoil.com`, owner's login, via claude-in-chrome). Mechanism: images are thumbor-proxied (`/thumbor/unsafe/fit-in/800x800/…`) over public CDN originals (`/cdn2/uploads/digitalLibrary/<uuid>/…`) — **fetchable server-side (no auth)** + thumbor converts AMSOIL's print-CMYK to web RGB. Folder IDs: Product Images parent **1221**, Gasoline Motor Oil **750**, Gear Lubricants **751**, Transmission Fluid **762**, Oil Filters **757** (SPA grid is virtualized → sweep the inner scroll container + accumulate; `find` tool locates category tiles). Saved to `site/images/amsoil/*.jpg`, catalog `.image` paths → `.jpg` (atl→atf). **Manifest `scripts/amsoil/image-sources.json` + refresh tool `scripts/amsoil/image-sync.mjs`** = the owner's **prompted-refresh** model (self-host, NOT runtime hotlink [dealer URLs fragile + break emailed certs], NOT auto-cron [discovering updated images needs the authed SPA; only *downloading* known URLs is public]).

- **Attribution deep-link ADDED (BUILT, tested green, ships with the above).** Answered owner's "how does AMSOIL know orders are mine?": the **`?zo=30713116`** on every outbound garage link (verified: Order buttons/shop/PC/dealer/search all via `amsoilUrl()`) → AMSOIL sets a **30-day device referral cookie** on landing → order credited. Gap: cert QR + follow-up email previously landed on the *garage* (no zo cookie until an Order click). FIX: `lib/amsoil-fluids.js` now exposes **`orderUrl` = `https://www.amsoil.com/shop/?zo=30713116`** (mirrors `AMSOIL_ZO`); the **cert QR** (`installer-closeout.js` + `certificate-dispatch.js`) and the **email primary CTA** (`lib/amsoil-email.js`) now point there so the cookie sets the instant they land; cert page-2 copy updated ("Scan to order at AMSOIL.com through Tuned Yota…"). The cert's printed fluid table remains the curation. Owner CONFIRMED **30713116** is the correct dealer/ZO.

- **⚠ OPEN ITEMS (owner asked to track):**
  1. ~~EA15K02 + EA15K49 filters missing images~~ **RESOLVED 2026-07-13 (master @ 0456af1)** — the 2 filters weren't in the dealer library's *image* folder, but they ARE valid orderable SKUs (order via the selector page `/p/amsoil-oil-filter-eaoilfilt/?code=<CODE>-EA`). Owner dropped the 2 images into gitignored `assets-source/Amsoil/` (`ea15k02-ea.jpg`, `ea15k49-ea.jpg`); processed to web RGB 800px + self-hosted. **ALL 13 AMSOIL product images now real + live.** Manifest marks these `ownerProvided` so `image-sync.mjs` skips them (not CDN-refreshable).
  2. ~~11 product images + zo deep-link~~ **SHIPPED LIVE 2026-07-13 (master @ d86c378)** — 11 images serve; cert QR + email CTA now → `amsoil.com/shop/?zo=30713116` (cookie on landing).
  3. **12 pre-existing completed bookings' certs** — **✅ SENT 2026-07-14** (script `scripts/resend-certs-backfill.js` `send` mode; each carries the tracked zo QR + PC pitch). All 12 marked `Cert Delivery=customer`, so `dry` mode now reports **0 eligible** (re-verified 2026-07-16 — nothing left to send; do NOT re-send). The earlier "READY TO SEND, awaiting owner's send" state below/above was superseded same day.

**AMSOIL CLICK TRACKING + PC CERT PITCH — SHIPPED LIVE 2026-07-14 (master @ 0f28634).** Front-C measurement foundation, built before the 12-cert batch so it's tracked from click one. **`netlify/functions/amsoil-go.js`** = a per-customer redirect: the cert QR + email link to `/.netlify/functions/amsoil-go?c=<bookingId>&to=shop|pc`, which logs the click (best-effort, fail-open) to the new **`AMSOIL Clicks`** Airtable table (`Booking`/`Customer`/`Destination`/`Clicked At`; created via metadata API, `tblCuODHaKMYUiZK4`) then **302s to amsoil.com with `?zo=30713116`** (referral cookie still sets on landing). Destinations centralized in `amsoil-go.js` (so a URL fix never needs re-issued certs). All 3 cert generators (installer-closeout, certificate-dispatch, resend-certs-backfill) build tracked `qrSvg(track("shop"))` + `pcUrl=track("pc")`. **Cert PC pitch upgraded** (`lib/certificate.js` amsoilPage): prominent **"Save up to 25% for life — become a Preferred Customer"** call-out linking the customer PC-registration (`amsoil.com/p/preferred-customer-registration-preg/?zo=`, via the tracked redirect) — **dropped the inaccurate "Enroll free"** (see [[amsoil-dealer-resources]]: PC terms unconfirmed, so fee-agnostic copy). Also added a **`PC Customer`** checkbox on Bookings for conversion reconciliation (owner matches AMSOIL dealer-portal PC signups → cert recipients; the dealer portal is the only source of final PC conversions — AMSOIL owns checkout). 551 tests green; amsoil-go 302 + click-logging verified live. **Still TODO (Front C):** the reconciliation workflow/report (certs sent → clicks → PC conversions) once real PC signups appear; and the 3-day follow-up EMAIL still links direct (not yet through amsoil-go) — wire it for email click-tracking next.

**STRATEGIC REFRAME — NATIONAL AMSOIL SALES (owner-directed 2026-07-13).** Owner: I'd under-indexed the biggest pillar — making tunedyota.com a **NATIONAL** AMSOIL destination (not just a Toyota/Lexus client tool). Target: **≥$15k/mo in AMSOIL sales within 3 months, $30k/mo year-end.** Analyzed 6 page-1 competitors (syntheticsusa/southsidelube/buyoildirect/thesyntheticgarage/cardey4everoil/switchsynthetics) — **ALL are identical `?zo=` referral funnels (no cart/inventory), price is fixed by AMSOIL → won on traffic+trust+conversion.** Their engines: content volume (BuyOilDirect's **31 product guides**; Synthetic Garage's **"Buy AMSOIL in [State]"** geo pages) + domain age + **PC-registration as the recurring money event**. NONE have vehicle authority, Product/FAQ/AggregateRating schema, reviews, or design quality — our opening. **Strategy = 3 fronts** (full doc `docs/marketing/amsoil-national-sales-strategy.md`): **A** own the Toyota/Lexus AMSOIL niche (deepen the 13 `amsoil-<make>-<model>` pages into buying guides w/ kits+schema+PC CTA — via `scripts/build-amsoil-pages.mjs`); **B** national content engine (product-guide library + geo pages); **C** convert-don't-leak (PC-registration as primary CTA + cert funnel). **Owner APPROVED: proceed Front A FIRST (fastest/most defensible), then immediately B then C.** PC enrollment URL for the CTA ≈ `amsoil.com/offers/pc/?zo=30713116`.

**FRONT A PHASE 1 — SHIPPED LIVE 2026-07-14 (master @ 9ed5ef4).** Upgraded all 13 `amsoil-<make>-<model>` vehicle pages (via `scripts/build-amsoil-pages.mjs`) into **conversion buying guides**: per-generation fluid cards now render the real **product image + live retail price + Order button** (`?zo=`), topped by a **"Recommended engine oil" hero card** (image, price, "Preferred Customers save up to 25%" + Order + Become-a-PC CTAs). Verified-capacity gate, Offer/FAQPage/Breadcrumb schema, and referral links all preserved; **deliberately NO per-product AggregateRating** (GSC manual-action risk per [[gsc-product-schema-warnings]] — trust via installer authority/dyno instead). Browser-verified (Tundra) + live. 548 tests green (guard `tests/amsoil-vehicle-pages.test.js` still passes).

**FRONT C — CLICK-SOURCE INSTRUMENTATION + ADMIN FUNNEL DASHBOARD — SHIPPED LIVE 2026-07-14 (master @ 3f0be81).** Turned the click tracking into a measurable funnel. (1) **`amsoil-go.js`** extended: sanitized **`s=` source tag** + validated **`p=` product path** (same-site-relative ONLY — regex + no-`//` guard, can't open-redirect; preserves each link's exact destination incl. `?code=` queries) + **tolerant `Source` column** logging via `createTolerant` (works before the column exists). (2) **`site/amsoil-track.js`** (NEW) = progressive-enhancement script now on **all 46 `amsoil-*.html` pages + garage** (emitted by `build-amsoil-pages.mjs` via a `TRACK` const after `${FQA11Y}` in all 3 templates): at runtime rewrites amsoil.com links → `/.netlify/functions/amsoil-go?to=&s=page:<slug>&p=<rel>`; **no-JS visitors keep direct links** (the rendered HTML is unchanged — schema Offer URLs stay direct too); a **MutationObserver** also catches the garage's dynamically-injected product links. (3) **`netlify/functions/amsoil-metrics.js`** (NEW, admin-only via `resolveInstaller`+`isAdmin`, 401/403 gated) aggregates the `AMSOIL Clicks` table + booking `Certificate Sent`/`PC Customer` flags into totals, clicks-by-source, the **certs→clicks→PC funnel**, a 14-day sparkline, recent clicks (pure `aggregate()` = testable). (4) **installer console** got an admin-only **"🛢 AMSOIL numbers"** header link → overlay rendering all of the above (`openAmsoilOverlay`/`renderAmsoil`). 575 tests green (amsoil-go/metrics/track); console render verified headlessly; live-verified (track.js 200, page instrumented, metrics 401, go 302→amsoil). **OWNER SETUP DONE 2026-07-14:** the **`Source`** single-line-text column was added to `AMSOIL Clicks` via the metadata API (ephemeral schema-token → clipboard flow; field `fldARhLjimYIDwxrA`). Keep marking **`PC Customer`** on bookings that convert (still the only PC signal — AMSOIL owns final conversion data).

**B1 3-DAY FOLLOW-UP EMAIL — ACTIVATED LIVE 2026-07-14.** Was inert; now fully on. `AMSOIL_FOLLOWUP_START=2020-01-01` (Netlify prod = "everything on record"); the 2 Bookings cols (`AMSOIL Email Sent`, `AMSOIL Opt-Out`) already existed + `Source` added (above); `amsoil-followup` was ALREADY scheduled in netlify.toml (daily `0 15 * * *` = 10am CDT — my earlier "not scheduled" read was a truncated grep). **GOTCHA that cost time:** the live deploy was STALE — a manual trigger returned `sent:0,skipped:12` (all skipped) while local `runAmsoilFollowup` sent 12; a forced redeploy (empty-ish netlify.toml doc commit `babc553`) fixed it and the very next trigger sent all 12. So: **after a `netlify env:set`, force a redeploy before trusting a scheduled/on-demand function** — env changes + (apparently) function code/bundle need the fresh deploy. All **12 backfill follow-ups SENT 2026-07-14** (same 12 who got certs; verified drained: 0 unsent, 12 stamped `AMSOIL Email Sent`, no duplicates since the first skipped-run wrote no stamps). Going forward the daily cron emails each new completed tune ~3 days post-tune. **3-day follow-up email NOW TRACKED (master @ d149a7a, 2026-07-14):** `lib/amsoil-email.js` shop + PC CTAs route through `/amsoil-go?to=&s=email&c=<bookingId>` (per-customer email attribution; ZO still sets via the redirect); also dropped the inaccurate "Enroll free" copy → fee-agnostic "save up to 25% for life" + made the PC line a real tracked link. NOTE: the follow-up sweep itself stays INERT until owner does B1 setup (2 Airtable cols `AMSOIL Email Sent`/`AMSOIL Opt-Out` + env `AMSOIL_FOLLOWUP_START`) — see [[certificate-v2-dashboard-program]]. **Still open (Front C):** the PC **spend tracker** awaits the owner's Dealer-Zone per-PC spend export.

**FRONT B PHASE 1 — national product-guide library STARTED & SHIPPING LIVE 2026-07-14** (masters @ 5fc37bc + c832190). Added a **data-driven guide generator** to `scripts/build-amsoil-pages.mjs` (module-scope `GUIDES` array + `guidePage()`/`guideCards()`, reusing the Front-A chrome/product-cards/schema; module-hoisted `priceOfP`/`imgTagP`). **6 guides LIVE** (each = a GUIDES entry → adding more is just data): `amsoil-synthetic-motor-oil-guide`, `amsoil-synthetic-atf-guide`, `amsoil-severe-gear-guide`, `amsoil-ea-oil-filter-guide`, `is-amsoil-worth-it`, **`amsoil-vs-oem-toyota-lexus-fluids`** (master @ 7e25c8d). **OEM-BARRIER FOCUS (owner's key strategic point 2026-07-14):** the #1 objection for our Toyota/Lexus audience is "just use OEM Toyota/Lexus fluid" — so the OEM guide breaks it (safe? won't void warranty per **Magnuson-Moss Warranty Act** + AMSOIL Limited Warranty; **meets AND exceeds** OEM perf standards; 75% more wear / 100% LSPI / 25k-mi drain / better value) AND a **make-aware OEM-safety FAQ was added to ALL 13 vehicle pages** ("Is AMSOIL safe for my {name}, or should I stick with {Toyota|Lexus} OEM fluid?"). Keep this OEM-vs-AMSOIL framing central in all future Toyota/Lexus AMSOIL content. Content is written from **AMSOIL's approved Dealer Sales Briefs** (all 37 downloaded to `~/Downloads/*.pdf`; read the ones needed — key claims + citations captured in [[amsoil-dealer-resources]]). Each guide: real product cards (image+price+`?zo=`), Store/OfferCatalog/FAQPage/Breadcrumb schema, PC "save 25% for life" CTA, vehicle cross-links; registered in `scripts/lib/seo-data.mjs` HEAD_PAGES + PRIORITY (0.8). 551 tests green; all 5 verified HTTP 200 live. **GEO PAGES SHIPPED LIVE 2026-07-14 (master @ 6d6a020):** 12 "Buy AMSOIL in [State]" pages (`amsoil-<state>.html`) via a `STATES` dataset + `geoPage()` in `build-amsoil-pages.mjs` — **substantive, NOT thin doorways** (real cities + a per-state climate/driving angle + AMSOIL value + product cards + Store/OfferCatalog/FAQPage schema + national-shipping FAQ + PC CTA). States: MN/IA/WI/ND/SD/NE (served — also surface tuning events + link the `toyota-lexus-tuning-<state>` page) + TX/FL/CO/AZ/MI/CA. Registered in seo-data. `export const AMSOIL_GEO_FILES`. **Expand by adding STATES entries** (kept it a curated quality set vs. 50 thin pages, to protect our rank). **SCALED 2026-07-14 (master @ 28dfc53):** added **3 per-viscosity guides** (`amsoil-0w20-guide`/`5w30`/`5w20` — each targets the grade keyword + which Toyota/Lexus engines use it, Signature-Series claims w/ footnotes) + **12 more states** (GA/OH/PA/IL/WA/TN/NC/MO/MT/ID/OK/IN → **24 geo pages total**). 551 tests green, all live. **NEXT (Front B):** more product guides (diesel, European, high-mileage, fuel additives, coolant — from the 37 downloaded briefs in [[amsoil-dealer-resources]]) + more STATES toward full national coverage. **Front C** (PC spend tracker) still awaits owner's Dealer-Zone per-PC spend export. **Front C:** the PC **spend tracker** (owner idea 2026-07-14 — nudge PCs toward the **$500/yr spend → free next-year membership**; needs a per-PC spend feed from the AMSOIL Dealer Zone [owner-export CSV → Airtable] since AMSOIL owns spend data; owner said "we can implement the data-in process") + PC-conversion reconciliation + wire the 3-day email through amsoil-go. **DONE:** the 12 backfill certs SENT 2026-07-14 (carry the tracked zo QR + PC pitch).
