---
name: search-ai-visibility-program
description: "Search + AI-visibility program (3 phases): Phase 1 content+AEO underway — entity/llms.txt LIVE, 4 content pages await owner copy; Phases 2-3 not started"
metadata: 
  node_type: memory
  type: project
  originSessionId: e75d74d1-a76c-483c-83d5-58cb118bc0c4
---

Owner wants to improve Google search performance AND AI visibility — all four goals (more bookings from organic/AI, rank for terms, be cited by AI engines, local visibility). Run as a **phased program** because content+AEO is one engine advancing the first three goals together. Spec/plan: `docs/superpowers/specs/2026-06-18-seo-aeo-phase1-content-design.md`, `docs/superpowers/plans/2026-06-18-seo-aeo-phase1-content.md`.

- **Phase 1 — Content + AEO foundation: IN PROGRESS.**
  - DONE & LIVE (2026-06-19): query map (`docs/seo/query-map.md`); founder `Person` entity on the homepage business schema; enriched `llms.txt` (citable Key Facts + FAQ). Guarded by `tests/seo-aeo.test.js`.
  - DONE & LIVE (verified 2026-06-27): all four high-intent pages — `ott-tune-cost`, `is-the-ott-tune-worth-it`, `magnuson-supercharger-guide`, `tune-warranty-emissions-legality` — are fully written (real copy, FAQ accordions + FAQPage schema, Service/Breadcrumb schema), registered in HEAD_PAGES, in sitemap, committed on master, and return HTTP 200. Briefs at `docs/seo/briefs/*.md`. The earlier "pending owner copy" status was stale. **Remaining = owner NUMBERS, not prose:** the pages deliberately punt where figures were unknown — `ott-tune-cost` shows "Call for pricing" for custom-calibration + supercharger rows, and `magnuson-supercharger-guide` claims NO specific power-gain figures (brief flagged these as high-value for ranking/citation). Fill these in only when the owner supplies real numbers (custom/SC pricing, Magnuson gains per platform, CARB-state nuances).
- **Phase 2 — Local: state pages now LIVE on master** (verified 2026-06-27, HTTP 200; GBP setup is still the owner's off-site task). 6 state landing pages `/toyota-lexus-tuning-{minnesota,iowa,wisconsin,north-dakota,south-dakota,nebraska}` generated by `scripts/build-state-pages.mjs` from markets.js (real cities + installers per state), local schema (Service areaServed cities), homepage "Where we tune" + Tundra region links. Off-site playbooks `docs/seo/gbp-setup.md` + `docs/seo/local-playbook.md` (owner executes — **GBP is the keystone** for geo queries; the diagnosis: Tuned Yota ranks ~#2 for "tuning in Wisconsin" mainly due to no GBP). **Shipped** — the earlier "blocked by Phase 1's supercharger-guide" note is resolved (both live). NOTE: `build-state-pages.mjs` was fixed 2026-06-27 (commit b1a3a28) to emit the current `site.css` chrome + skip-link/#main + FAQ aria script — it previously regressed the [[ux-accessibility-pass]] work on regen; safe to re-run now. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-19-seo-aeo-phase2-local*.md`.
- **Phase 3 — measurement engine: BUILT (measurement-first sub-round). On-page CTR + internal linking: NOT STARTED.**
  - **Measurement engine** (spec `docs/superpowers/specs/2026-06-30-seo-aeo-phase3-measurement-design.md`, plan `docs/superpowers/plans/2026-06-30-seo-aeo-phase3-measurement.md`): merged to LOCAL master 2026-06-30 (merge commit 3039765, 177 tests green) — **NOT yet pushed/deployed**. Pure tested libs under `scripts/measure/lib/` (tracked-queries, gsc, perplexity, snapshot+CTR-curve+diff, report) + thin CLIs under `scripts/measure/` + tracked-query set `docs/seo/tracked-queries.json` + routine playbook `docs/seo/measurement-routine.md`. Flow: GSC API pull + Perplexity API probe + Claude WebSearch probe → dated JSON snapshot in `docs/seo/measurements/` → trend diff → Slack report. Also declared the previously-undeclared **`sharp`** dep (a stray `npm install` had pruned it and broke `build:seo` — now prune-safe). **Owner setup progress:** ✅ GCP service account `gsc-reader` created + GSC read granted + JSON key **verified pulling live data 2026-06-30** (via `node scripts/measure/gsc-pull.mjs` — returned real tracked queries + topPages). `GSC_PROPERTY` = **URL-prefix `https://tunedyota.com/`** (not domain). Key file lives in owner's Downloads (`tunedyota-*.json`) — becomes routine secret `GSC_SA_KEY`. ✅ Cloud routine **`TunedYota — Search & AI Visibility Tracker` CREATED 2026-06-30** (`trig_01SCZCpg13QpSvsmquEcagGe`, cron `0 13 1 * *` = 1st of month 8am CDT, model sonnet-4-6, tools incl WebSearch). Owner chose the CLOUD-routine-with-embedded-key model (over a local Windows-Task-Scheduler run) — so the GSC SA JSON key + Slack webhook are embedded in the routine PROMPT (created with `«PASTE_…»` placeholders to keep secrets out of the chat transcript; owner fills them in the claude.ai UI). This cycle is a GSC + WebSearch **baseline** — Perplexity step stubbed as `echo "[]"` until the key exists. Snapshot persisted via `git push` to master each run (monthly inert deploy) so the diff can trend. **BASELINE DELIVERED 2026-07-01**: after fixing that the GSC-key + Slack-webhook placeholders hadn't actually saved (only the Perplexity line had) + a doubled `pplx-pplx-` key prefix, a UI-triggered run posted the report to Slack — full pipeline (GSC + WebSearch + Perplexity + snapshot + report) proven in prod. Perplexity key now embedded (step 2 uses real probe). **Gotchas learned (IMPORTANT):** (1) `RemoteTrigger` run/get **echoes the whole routine prompt incl. embedded secrets** into the session transcript — so the Perplexity key LEAKED → **ROTATE IT** (still pending); and going forward the OWNER must trigger runs from the claude.ai UI, not Claude via RemoteTrigger. (2) The cloud routine's **GitHub integration was READ-ONLY** — `git push` / create-ref / PR all 403'd ("Resource not accessible by integration"), so the snapshot committed locally but never persisted; owner **granted the Claude/Anthropic GitHub App write access 2026-07-01** to fix persistence (also likely unblocks the other routines' PR steps). **PIVOTED TO A LOCAL RUNNER 2026-06-30** (owner fed up with cloud-routine friction: secret-embed-in-prompt, RemoteTrigger echoing secrets, read-only GitHub integration). New `scripts/measure/run-local.mjs` (master @ dfdeb29+) reads secrets from `~/.tunedyota/measure.config.json` (gscKeyFile path + gscProperty + optional perplexityApiKey + optional slackWebhookUrl — file is OUTSIDE the repo, never committed), pulls GSC + Perplexity → snapshot → Slack → `git commit && git push` with the owner's own creds (persistence just works). WebSearch presence probe DROPPED locally (cloud-agent-only; GSC already gives exact Google positions) — `report.mjs` now omits a probe's stat when it didn't run. **BASELINE PULLED + PUSHED LOCALLY** (`docs/seo/measurements/2026-07-01.json`, real GSC data: tundra-ott pos 7.1/16impr, what-is-ott pos 8.2/33impr, ott-cost pos 30/1impr; 13 topPages). Scheduled via Windows Task Scheduler task **`TunedYota Search Visibility`** (monthly, 1st 08:00) → wrapper `~/.tunedyota/run-measure.cmd` → logs to `~/.tunedyota/measure.log`. Doc: `docs/seo/measurement-local.md`. **FULLY LIVE 2026-07-01**: config now has the rotated Perplexity key + Slack webhook (owner's Notepad/Explorer saves kept missing the real file — the `.tunedyota` dot-folder was the culprit; wrote both via `Get-Clipboard` in the PowerShell tool after owner copied each). Full run green: GSC 3 tracked + 13 topPages, **Perplexity cited 5/16 (31%) baseline**, report posted to Slack (HTTP 200), snapshot committed + pushed. **Both cloud routines DELETED by owner 2026-07-01** (the abandoned cloud `search-visibility-tracker` `trig_01SCZCpg13QpSvsmquEcagGe` AND the old GSC reminder `trig_014SdCp5ifkJ2hPwmnwgt8gj`) — no double-runs. Perplexity key was rotated before final config. **NO OPEN ITEMS** — the measurement engine is fully live locally and self-running monthly. Next *build* step (later, unstarted) = the on-page CTR + internal-linking round, fed by `summary.ctrOpportunities`. Lesson for any future cloud routine: `RemoteTrigger` run/get/update echoes the whole prompt incl. embedded secrets into the transcript, and the cloud GitHub integration is read-only (can't push/PR) — prefer a LOCAL runner with env/file secrets + the owner's own git creds. See [[cloud-routines]].
  - **On-page round (NOT started):** title/meta rewrites for click-through + internal hub-and-spoke linking. Deliberately deferred — it is fed by the engine's `summary.ctrOpportunities` (page-1, high-impression, below-CTR-curve queries), so it wants a baseline snapshot or two first.

Builds on the already-live technical SEO from the [[seo-generator]] + the post-launch SEO pass (schema, sitemap, robots+AI crawlers, OG, GSC). A 2026 AEO/GEO best-practices research pass was offered but the owner chose to proceed without it.
