---
name: certificate-v2-dashboard-program
description: Installer-dashboard enhancement program — sub-project A (Certificate v2) SHIPPED 2026-07-13; B/C/D roadmap + owner pending tasks
metadata: 
  node_type: memory
  type: project
  originSessionId: 243af81c-910b-41d5-b689-e96648fcaec0
---

A multi-part program to evolve the Installer Console (`/installer.html`) that the owner scoped 2026-07-13. Decomposed into 4 sub-projects, each its own spec→plan→build.

**Docs:** the everyday console manual is `docs/operations/installer-dashboard-playbook.md` (backs SOP 4 Close-Out + SOP 6 Field Guide, both refreshed 2026-07-13 for the smart-feed redesign + everyday walk-ins). Cert v2 spec/plan: `docs/superpowers/specs/2026-07-13-certificate-v2-design.md`, `docs/superpowers/plans/2026-07-13-certificate-v2.md`.

**Sub-project A — Certificate v2 — SHIPPED LIVE 2026-07-13** (master @ ebc9cb2). Two-page cert (page-2 AMSOIL Maintenance Reference w/ fluids + stock numbers + order QR), direct-to-customer delivery, searchable cert repository (`installer-certificate.js`), walk-in/close-out email capture, AMSOIL Garage landing enhancements. Full detail in [[tuned-yota-master-certificate]] + [[amsoil-garage-program]].

**OWNER SETUP — DONE 2026-07-13:**
1. ✅ 3 Airtable **Bookings** columns added + **write-test CONFIRMED** (create/delete a test record accepted `Certificate Issued` Date, `Certificate Recipient` Text, `Cert Delivery` single-select `customer` — exact names match the code). Metadata/repository now persist.
2. ✅ AMSOIL **stock numbers** accepted as-is by owner ("completed for now").

**Garage `?year=` prefill + gate — DONE, SHIPPED LIVE 2026-07-13 (master @ 4e3598a):** (a) **Layer A** — `applyPrefill` now matches the exact `?year=` against the range-label options via `AmsoilGarage.inRange()` (reused, not a new helper), so the year pre-selects (e.g. `?year=2024` → "2024+"). (b) **Layer B** — the `/amsoil-garage` landing's `verified` gate was RELAXED to match the cert: `resolveVehicle` gained an opt-in `includeUnverified` 4th param (default stays gated so the tested contract + any future caller are unchanged; hub passes `true`), and `yearsFor`/`sync` dropped their `g.verified` filter. The landing now shows the SAME per-vehicle fluids the certificate prints. Browser + live verified (Tacoma 2.4L-T fluids render, year pre-selects). **NOTE — remaining inconsistency (deliberately out of scope):** the 13 per-vehicle SEO pages (`amsoil-<make>-<model>.html`, built by `build-amsoil-pages.mjs`) STILL withhold unverified capacities (test-guarded `capacity-withheld` invariant in `tests/amsoil-vehicle-pages.test.js`) — a separate indexed surface; relaxing those is a distinct SEO/accuracy call the owner hasn't made. See [[amsoil-garage-program]].

**Roadmap — approved by owner, NOT yet built (2026-07-13):**
- **B · Smarter close-out** (4 features, each its own spec→plan→build):
  - **B1 · AMSOIL attach — SHIPPED LIVE 2026-07-13** (master @ 6735a71). NOT a close-out code change — the "opportunity" IS the completed booking. `netlify/functions/amsoil-followup.js` (daily scheduled sweep `0 15 * * *` ≈10am Central, mirrors certificate-dispatch: idempotent via `AMSOIL Email Sent` stamp, Slack on failure) + pure `netlify/functions/lib/amsoil-email.js` builder emails each customer their tailored AMSOIL fluids + PC "save 25%" + `?zo=` garage link **~3 days after the tune**. Backfill floor via env `AMSOIL_FOLLOWUP_START`; reply-based opt-out honored via `AMSOIL Opt-Out` checkbox. Spec/plan: `docs/superpowers/{specs,plans}/2026-07-13-amsoil-attach-followup*`. **ACTIVATED LIVE 2026-07-14** — owner setup done (both Bookings cols existed; env `AMSOIL_FOLLOWUP_START=2020-01-01` = everything); 12 backfill follow-ups sent + drained. Full activation writeup (incl. the STALE-DEPLOY gotcha: force a redeploy after `netlify env:set`) is in [[amsoil-garage-program]]. (Also a precursor to [[amsoil-garage-program]] P3 mileage/service-due reminders.)
  - **B2 · VIN decode + guard — SHIPPED LIVE 2026-07-13** (master @ 9d86add). Auth-gated `netlify/functions/vin-decode.js` proxies NHTSA vPIC + `netlify/functions/lib/vin-guard.js` (pure `compareVin`) warns the installer at close-out when the entered VIN mismatches the booking — **3 checks: check-digit typo, year, make/model** (aftermarket SC NOT checked — VINs can't see it). Console shows an amber warning + requires a "✔ I've verified this VIN is correct" checkbox to unlock Mark-complete (the manual override). **Advisory only — nothing stored, NO owner setup** (no Airtable/env; `INSTALLER_TOKENS` gates the proxy). **NHTSA gotcha (baked in):** latency is bimodal (sub-second OR 20s+ spikes) → **8s fail-open timeout** (under Netlify's ~10s sync limit → clean `unavailable`, not a 502); guard is best-effort (skips silently when NHTSA is slow, never blocks). `modelYear` added to installer-roster payload for the year check. Spec/plan: `docs/superpowers/{specs,plans}/2026-07-13-vin-decode-guard*`.
  - **B3 · mileage photo + odometer OCR — DESIGNED & BUILT, then REVERTED 2026-07-13 (owner backed off).** Was: 📷 at close-out → Claude Haiku 4.5 vision reads the odometer → pre-fills Mileage (override) → photo attaches to the booking row (Airtable upload-attachment). Fully built in an isolated worktree (odometer-vision.js + read-odometer.js + installer.html photo button, 12 tests green) but **the owner chose to back off the photo/vision API before ship — reverted to plain MANUAL mileage entry** (the pre-existing `mi_<id>` field, untouched on master). Worktree + 3 code commits discarded; spec/plan removed from master (@ c67a27d). **Why:** owner decision — it was the first feature adding a per-use cost (Haiku vision call) + an external dep (Anthropic API) into close-out. Do NOT re-propose photo/OCR mileage unless the owner revisits. (Policy 0009 had been cleared for a records-only photo; ANTHROPIC_API_KEY + a `Mileage Photo` Airtable column were never added.)
  - **B4 · digital customer sign-off — SHIPPED LIVE 2026-07-13** (master @ 7983ea3). A **drawn** customer satisfaction signature captured at close-out — proof of acceptance, **record-only** (NOT on the certificate, does NOT trigger it). Flow: at **Mark complete**, after the existing validation, a `openSignOverlay` full-screen pad (vanilla `<canvas>`, no library; reuses the review-overlay `reviewov`/`reviewbox` classes) opens; **✓ Done & complete** captures `toDataURL('image/png')` (only if the pad is `dirty`) and rides it through the existing `closeout` POST as `signature`; **"Customer unavailable — skip →"** and the ✕ complete/cancel with no signature — **never blocks completion**. `installer-closeout.js` tolerant-writes it to a new Bookings col `Customer Signature` (PNG-data-URL guard + 200KB cap; appended to the `updateTolerant` drop-list → inert until the col exists; the `Certificate Sent` idempotency early-return means it's captured on the genuine first close-out). `installer-roster.js` exposes `signed` (boolean; **image never in the feed**). New authed `netlify/functions/installer-signature.js` (`getSignature`) returns a stored signature scoped like the roster (owning installer, or admin any → else 403). Completed cards show **✍ Signed — view** → `viewSignature` fetches + renders the data-URL `<img>`. 540 tests green; endpoint 401 + installer.html 200 verified live. **⚠ OWNER SETUP (one thing, feature inert until done — no risk):** add Airtable **Bookings** column **`Customer Signature`** (Long text). No env vars. Spec/plan: `docs/superpowers/{specs,plans}/2026-07-13-digital-signoff*`.
- **C · Dashboard ops:**
  - **C1 · live commission tracker — SHIPPED LIVE 2026-07-13** (master @ 002cff5). Console header shows a running **full calendar-month OTT price-sheet commission** total = the same `resolveCommission` figure the monthly report uses (**OTT commission owed, NOT installer take-home** — owner confirmed). `installer-roster.js` resolves per-completed-booking `commission` server-side (roster is scoped, so a regular installer only ever gets their own amounts → privacy automatic; price sheet never sent to client). Pure shared `site/commission-tally.js` (browser+Node dual-export like amsoil-referral.js) sums month `{total,tunes,pending}` + last-month + lifetime + by-installer. `renderTally` shows "This month · est. $X · N tunes (+K pending)" + lifetime commission for everyone; **admin-only** (INSTALLER_ADMINS = Aaron) also gets a per-installer roll-up + "Last month · est. $Y — report due by the 7th" (Policy 0007). Always "est." + ambiguous (null) tunes shown as **pending** (never overclaims vs the confirmed report). No Airtable/env/owner setup. Browser-verified both admin + installer views. Spec/plan: `docs/superpowers/{specs,plans}/2026-07-13-commission-tracker*`.
  - **C2 · Google-review QR — SHIPPED LIVE 2026-07-13** (master @ 877a2ac). Top-level "★ Ask for a review" link in the console header → full-screen overlay with a QR the customer scans to open the Tuned Yota Google review form (DASHBOARD-only, NOT on the certificate — owner directive). Public `netlify/functions/review-qr.js` renders `env.GOOGLE_REVIEW_URL` as an SVG QR via the existing `lib/qr.js`; roster exposes `reviewUrl` so the button self-hides until configured (404 + no button when unset — verified live). No rating pre-fill (Google policy — QR opens the review form, customer picks). **⚠ OWNER SETUP (feature invisible until done):** set Netlify env **`GOOGLE_REVIEW_URL`** = the Google Business "Ask for reviews" share link (e.g. `https://g.page/r/…/review`). Spec/plan: `docs/superpowers/{specs,plans}/2026-07-13-google-review-qr*`.
  - **C3 · web push notifications — SHIPPED LIVE 2026-07-13** (master @ 9d8ff97). Browser-native web push (Push API + VAPID) to installers' phone browsers — chosen over the dormant FCM path (FCM needs the unshipped native app, so it's inert). New `netlify/functions/lib/webpush.js` `sendWebPush(installerKey,{title,body,url},deps)` sends via the `web-push` npm dep, reading the installer's subscriptions from a new **"Web Push Subs"** Airtable table + auto-deleting 404/410 (expired) subs; non-blocking (returns `{sent,failed}`, never throws). `site/sw.js` service worker (showNotification + open-on-tap). `push-subscribe.js` (auth, upsert sub deduped by endpoint) + `push-test.js` (auth, self-test send). Roster exposes `vapidPublicKey`; console shows a "🔔 Enable notifications" header link (register SW → permission → subscribe → POST) that flips to "Send test" — self-hides when VAPID unset or browser unsupported. **4 non-blocking triggers** each `sendWebPush` at an existing hook: (1) new booking `book-background.js` beside the FCM push; (2) day-of roster `event-reminders.js` (daysUntil===0, added a `plan` injection seam for testability); (3) certificate held/blank-calibration `certificate-dispatch.js`; (4) OTT report due `ott-report-reminder.js` → admins (`INSTALLER_ADMINS`). 530 tests green; sw.js 200 + both endpoints 401 verified live. **iOS caveat:** web push only when the console is Added to Home Screen (installed PWA); Android works in-browser. **⚠ OWNER SETUP (feature inert until done — no-ops safely meanwhile):** (a) Netlify env `VAPID_PUBLIC_KEY` (public key `BEMZfgw1MRmXLeJn1U0ReKd4-uwESxLPG4I657rYT-qktSSvz89fwGJ73jNP3HXvf2XQS4m3by4jt_v90v4hFtI`), `VAPID_PRIVATE_KEY` (SECRET — generated this session, routed to owner via clipboard, NOT stored here), `VAPID_SUBJECT=mailto:info@tunedyota.com`; (b) new Airtable **"Web Push Subs"** table with columns `Installer` (text), `Endpoint` (text), `Subscription` (long text). Spec/plan: `docs/superpowers/{specs,plans}/2026-07-13-web-push-console*`.
  - **C4 · installer console PWA / offline hardening — SHIPPED LIVE 2026-07-13** (master @ faa419e). Full PWA so the console works through thin/no event signal. **Read path:** dedicated `site/installer.webmanifest` (`start_url`/`scope` `/installer.html`, standalone → the INSTALLED app opens to the console, not the homepage; site-wide `site.webmanifest` untouched); `site/sw.js` extended with a shell cache (`CACHE_VERSION` + `SHELL` list, `install`/`activate`/`fetch` **stale-while-revalidate**; ONLY intercepts the console shell — public site + ALL `/.netlify/functions/*` pass through UNcached; push handlers preserved verbatim); `installer.html` registers the SW on load (not just on push-enable), and `load()` is now network-first with a `localStorage['ty_roster_cache']` fallback + an ⚠ offline banner so the feed shows the day's bookings cold-offline. **Write path:** pure testable `site/offline-queue.js` (dual export like commission-tally.js: `makeOp`/`shouldQueue`/`nextFlushResult`/`loadQueue`/`saveQueue`, key `ty_pending_ops`); `closeout()` + `addWalkin()` queue on **network failure or 5xx ONLY** (never 4xx) with optimistic UI (booking→Completed / walk-in card, tagged ⏳ pending) + "will sync when back online"; `flushQueue()` replays oldest-first (2xx remove · 401 stop+re-login · 5xx/network retry-later · other-4xx drop-poison), triggered by the `online` event, every `load()`, a manual **⏳ N pending sync** header badge, and Background Sync (`ty-flush` SW→client postMessage) where supported (iOS lacks it → foreground triggers cover it); `beforeunload` + logout confirm guard against discarding unsynced ops. **Walk-in dedupe:** each walk-in carries a client `clientKey`; `installer-walkin.js` gained a `list` dep + `Client Key` lookup (best-effort, swallows missing-column) to return an existing booking instead of duplicating on replay, and tolerant-writes `Client Key`. Close-outs need no key (idempotent via `Certificate Sent`). 548 tests green; manifest/sw/offline-queue all 200 live. **⚠ OWNER SETUP (one col, dedupe-only — queue+offline work without it):** add Airtable **Bookings** column **`Client Key`** (Single line text). Spec/plan: `docs/superpowers/{specs,plans}/2026-07-13-installer-pwa-offline*`. **UPDATE 2026-07-14 (master @ d5d0710, `sw.js` `CACHE_VERSION=ty-console-v2`):** the SW nav for `/installer.html` is now **NETWORK-FIRST** (cache only as offline fallback), NOT stale-while-revalidate — the SWR nav made every shipped console fix appear only on the installer's SECOND reload. Static assets (icons, `commission-tally.js`) stay SWR. Also that session: fixed the walk-in add UI being unreachable while searching / on finished "Recent" events (@ 6d0c03a), and — the headline fix — **the roster fetch could hang forever on "Loading…"** (no timeout); `load()` now has a 15s `AbortController` timeout + cache-or-**Retry** fallback, and `buildRoster` bounds the best-effort events load at 6s (`eventsTimeoutMs`) so a stalled Google Sheet can't hang the whole function (@ 160dede). Verified: all 3 installers' rosters build healthy & fast server-side (cody 1.2s/aaron 246ms/noah 182ms) — the hang was purely the client's missing timeout, so it could hit any installer. **UPDATE 2026-07-14 pm (master @ a3f317d, `sw.js` `CACHE_VERSION=ty-console-v3`):** fixed TWO field bugs where a 2nd consecutive walk-in failed after the 1st worked (Cody, live). (1) **Infinite roster-fetch loop** (live since a9d0301, 07-13): `flushQueue()` reloaded the roster whenever the queue was empty+online — even having synced NOTHING — and `load()` calls `flushQueue()` every load; the unawaited re-entrant `load()` cleared the `FLUSHING` guard before its own flushQueue ran → `load→flushQueue→load` recursed forever (~480 roster fetches/sec measured in Chromium), and the constant `renderAll()` destroyed the walk-in form inputs mid-entry so a 2nd walk-in couldn't be completed. Fix = new pure guard `shouldReloadAfterFlush(synced,remaining,online)` in offline-queue.js: reload ONLY after actually syncing ≥1 op. **Don't reintroduce an unconditional post-flush `load()`.** (2) **Silent city misfile:** the anyday "Log a walk-in" city field was a `<select>` limited to roster cities, so a walk-in in a market not yet on the roster was silently filed under the wrong (default) city — now a **free-text input + `<datalist id=ty_walk_cities>` suggestions** (any market enterable; server validates → loud "not a recognized market"). **Keep it free-text; don't revert to a select.** Regression tests: offline-queue guard unit tests + `tests/installer-walkin-browser.test.mjs` (Playwright — no-loop / different-city / three-consecutive / unknown-city). 581 tests green; all 3 markers live on prod.
- **D · Identity & "users":** installer login via **web + app**, and clients as first-class app/website **"users"** — the app program's identity layer + runway to the commerce phase and the account-backed persistent AMSOIL garage (P2). Foundational; deliberately last so A–C ship on today's passcode. **FIRST HALF (client accounts: magic-link `/account` portal, certs + synced My Garage) CODE SHIPPED LIVE 2026-07-17** — see [[client-accounts-program]] (Clients table + live E2E pending owner). Installer identity = the remaining half.

**Discarded:** before/after Dynojet capture at close-out (owner: "does not apply at this time").

**Assets:** official AMSOIL brand kit lives in **gitignored** `assets-source/Amsoil/` (logo files + Brand Style Guide PDF); the deployable logo is `site/images/amsoil/amsoil-logo.png` (unaltered, on white per guide). Related: [[tunedyota-app-program]], [[ott-policy-standards]], [[monthly-ott-calibration-report]], [[installer-console-access]].
