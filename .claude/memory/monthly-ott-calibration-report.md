---
name: monthly-ott-calibration-report
description: "Project to auto-report all completed OTT calibrations to OTT monthly; VIN-capture prerequisite is shipped, report build pending OTT contact email"
metadata: 
  node_type: memory
  type: project
  originSessionId: b8226a9f-fe8a-4123-94df-a3f041eace32
---

Owner wants a **monthly report to OTT of all completed calibrations**, delivered as an **email to an OTT contact** (owner's chosen delivery, 2026-07-02), fitting the existing Netlify-function infra (sibling to `submissions-report.js`).

**Prerequisite SHIPPED 2026-07-02 (master @ 1a03dda, live + verified):** VIN is captured at installer close-out and stamped on the certificate. Installers enter a required, validated 17-char VIN on `/installer.html`; it's normalized (uppercase, strip non-alphanumeric) in `installer-closeout.js`, written to Airtable Bookings, rendered on the Certificate of Calibration (`lib/certificate.js` VIN row) and the daily `certificate-dispatch.js` backstop. New `updateTolerant()` in `lib/airtable.js` means a missing VIN column never blocks a close-out. Canonical design `docs/brand/tuned-yota-master-certificate.html` kept in sync. +5 tests (224 total green).

**DONE 2026-07-02:** the **`VIN`** column (Single line text) was added to Airtable **Bookings** by the owner and CONFIRMED live via the data API (`fields[]=VIN` → 200). NOTE: the `AIRTABLE_TOKEN` still 403s on Airtable's **metadata API** even after the owner added `schema.bases:read/write` — the scope grant didn't take effect on Netlify's token (likely a different/old PAT stored in Netlify, or Airtable PAT scope edits not applying). Not blocking: verify schema via the DATA api (`?fields[]=Col`) instead of the metadata api, which works with the existing token.

**Monthly report SHIPPED & LIVE 2026-07-02** (master @ 5395f8a; approve endpoint verified HTTP 401 fail-closed; `OTT_APPROVE_SECRET` set in Netlify). First scheduled draft fires Aug 1. Built to owner's 4 rules:
- **Rule 1 — owner approves before any OTT send.** `netlify/functions/ott-report.js` (scheduled 1st) DRAFTS to owner (info@tunedyota.com) only, with a private token approve-link + CSV; never emails OTT. `netlify/functions/ott-report-send.js` (HTTP, token-gated by `OTT_APPROVE_SECRET`) is the approve endpoint → sends to OTT. Pure builders in `lib/ott-report.js`.
- **Rule 2 — recipients** = `info@overlandtailor.com` + `hgobbels@me.com` (CC info@), overridable via `OTT_REPORT_TO`. (Earlier `reports@ott.com` was WRONG — corrected.)
- **Rule 3 — annual per-calendar-year rollup, private to info@tunedyota.com** — NOT built yet (waiting on pricing to include cost totals).
- **Rule 4 — price per calibration** — owner uploading an Excel; NOT received yet. BLOCKS pricing columns + annual cost totals.
Report source = Airtable Bookings where `Status="Completed"` AND `OTT Calibration` set AND `Calibration Date` in month. 235 tests green. **Go-live needs:** set `OTT_APPROVE_SECRET` (random) in Netlify, finalize pricing+annual from the Excel, owner approval, then ship + update `docs/operations/sop-monthly-ott-report.md` (still describes old auto-send design). See [[tuned-yota-master-certificate]], [[event-reminders-automation]], [[funnel-roadmap-and-lead-setup]].

**EXPANDED into a 3-track OTT program 2026-07-02** (owner fed 3 reference docs + strict format):
- **Format LOCKED** — OTT submission = 14 cols in exact order: Date of Submission · Date Calibration Applied · OTT Retailer · Customer First Last Name · VIN · Vehicle Year · Vehicle Type(12-val) · Engine Size · ECU ID · Gear Size · Mileage · Tuning Platform(VFT/BB/PCM/HPT) · Calibration Type(Basic/Supercharger/CARB Update/MAF/9.2 New/9.2 Update/TCM Update/Custom/K-Line/combos) · Commission. Source = Master OTT Tracker "Blank- Formatted"/"Definitions" sheets. OTT-facing → NO TY brand rules (Stage 0-3/MAF allowed).
- **Owner's 3 build decisions:** commission = AUTO-LOOKUP from price sheet + owner confirms; close-out = DERIVE vehicle basics + installer adds rest; output = FILLED .xlsx workbook.
- **Reference data captured (repo working tree, uncommitted):** `netlify/functions/lib/ott-commission-template.json` (April price sheet — OTT Commission $ per config, COBB excluded) + `docs/ott/master-reference-5.7L.json` (5.7L cal-ID cross-ref: Tundra 130 rows + Sequoia/LC/LX 46 rows; resolves New Cal ID/TSB/CUW).
- **Track A** = monthly commission submission (draft→approve→send already built; needs the 14-col .xlsx + commission engine + close-out capture fields). **Track B** = installer SOP + per-vehicle calibration reference (the "installation guide" — Master Reference Sheet is slice 1). **Track C** = annual private rollup.
- **Track A progress SHIPPED 2026-07-02 (master @ c32986c):** commission engine `lib/ott-commission.js` (deriveVehicle from booking text + lookupCommission from price sheet; ambiguous→candidates for owner confirm) + reference JSONs committed (repo confirmed PRIVATE) + `docs/ott/README.md` (14-col format spec). 6 engine tests green (241 total). NOT yet wired into the report/close-out. Owner has 3 Airtable tokens (clipboard reads flaky — one is a valid schema token that briefly worked, others 401/wrong). Manual field specs given.
- **Close-out capture LIVE 2026-07-02 (master @ 64c02fb):** installer console `/installer.html` now captures Tuning Platform, Calibration Type, ECU ID, Gear Size, Mileage per completed truck (required in UI; server-optional + tolerant; NOT on the customer cert). The **5 Airtable columns were added MANUALLY by owner** (Tuning Platform singleSelect VFT/HPT/PCM/BB · Calibration Type singleSelect Basic/MAF/Basic+MAF/Supercharger/CARB Update/9.2 New/9.2 Update/TCM Update/Custom/K-Line · ECU ID text · Gear Size text · Mileage number) — all 5 verified present via data API (`?fields[]=`). Deployed for the Fargo Jul 3 event. NOTE: Airtable metadata API is UNUSABLE (Netlify token lacks schema scope; owner has 3 tokens, clipboard kept truncating PATs to 17 chars/401) → verify schema via DATA api, add fields MANUALLY.
- **TRACK A COMPLETE + LIVE 2026-07-02 (master @ 7b2d13d):** monthly report now emits OTT's exact **14-column .xlsx** submission via `lib/xlsx-writer.js` (dependency-free STORE-zip + inline-strings writer; validated openpyxl-loadable — NO npm dep needed). `lib/ott-report.js` `buildSubmissionRows` maps completed calibrations → 14 cols (Vehicle Type/Year/Engine DERIVED from booking text; ECU/Gear/Mileage/Tuning Platform/Calibration Type from close-out; Commission auto-resolved via `lib/ott-commission.js`, null+flagged when ambiguous/BB). Flow: `ott-report.js` (sched 1st) drafts .xlsx to owner + approve link → `ott-report-send.js` (token-gated, verified 401 fail-closed live) emails OTT (info@overlandtailor.com + hgobbels@me.com, CC info@) stamping Date of Submission. Draft flags rows needing commission confirm; owner fills those cells in the attached sheet. First scheduled draft = Aug 1. 242 tests green. env: `OTT_APPROVE_SECRET` set; `OTT_RETAILER` optional (default "Tuned Yota"); `OTT_REPORT_TO` optional override.
- **TRACK C COMPLETE + LIVE 2026-07-02 (master @ 7f735b3):** annual private rollup, TY-internal, NEVER to OTT. `lib/ott-annual.js` `buildAnnual(bookings, year)` → cost totals (sum of OTT commission) + breakdowns by month/installer/vehicle type/tuning platform/calibration type + Detail list, recomputed from Bookings. `lib/xlsx-writer.js` generalized to multi-sheet (`buildWorkbook`) → Summary + Detail workbook (validated openpyxl). `ott-annual.js` = SCHEDULED Jan 1 (prior year) — emails info@ only. `ott-annual-run.js` = token-gated HTTP on-demand (`?year=YYYY&token=OTT_APPROVE_SECRET`) for current YTD/any year, verified 401 fail-closed live. KEY LESSON: Netlify scheduled functions are NOT HTTP-invokable (return 404) → any on-demand path needs a SEPARATE non-scheduled HTTP function (mirrors ott-report scheduled + ott-report-send HTTP). 247 tests green.
- **TRACK B COMPLETE + LIVE 2026-07-02 (master @ bff19ec):** installer calibration reference. `lib/calibration-reference.js` `lookup({model,year,drivetrain,fuelTank,tow,flex})` → New Cal ID/Old Cal/TSB/CUW, + `coverage()` (KNOWN = 5.7L Tundra/Sequoia/Land Cruiser incl LX570-folded; PENDING = 23 (platform,engine) combos computed live from the price sheet — every non-5.7 engine: 2.4L/2.7L/3.5L/4.0L/4.6L/4.7L across Tacoma/4Runner/FJ/GX/Highlander/Camry/RAV4/RX350/LS460/LC250/Tundra-non-5.7). `calibration-reference.js` = installer-token-gated endpoint (verified 401). `site/calibration.html` = noindex passcode-gated console page (Model→Year→optional filters), linked from /installer.html. Data at `netlify/functions/lib/calibration-reference-5.7L.json` (code copy) + docs/ott/. 250 tests green. **BROWSER-VERIFIED 2026-07-02** via claude-in-chrome on the LIVE page: model→year dependency, Tundra 2007 → 4 correct New Cal IDs (30C57400/58400/59400/60400) w/ CUW badges, drivetrain=2WD narrows to 2, Old Cal/TSB render, coverage panel shows known-vs-pending. TECHNIQUE (reusable for any installer-token-gated page): don't pull the installer passcode into the transcript — stub `window.fetch` with the real data (dump a slice via node from the lib) + call the page's `showApp()`/change handlers to exercise real load→filter→render; gate itself confirmed separately by curl (401 no-token / 200 page). To do a real through-the-gate run instead, owner unlocks /installer.html once and /calibration.html reuses the stored `ty_installer_token`. **OWNER TO PROVIDE: the cal-ID cross-reference data for the 23 pending platform/engine combos** — then merge + coverage shrinks. **SCHEMA SIMPLIFIED for the 23 (owner, 2026-07-07): Key/Push, Fuel Tank, Tow, Flex, and TSB DO NOT APPLY to the non-5.7L combos.** Per-row shape for these = `Year · Model · Trans · Engine Size · Drivetrain · Old Cal ID · New Cal ID · CUW` only. Core values still needed = Old→New Cal ID mapping + CUW flag (the actual OTT data, not derivable). Lookup/coverage code already tolerates the dropped columns being null — no code change needed; just append rows to the reference JSON when data arrives. **WHOLE OTT PROGRAM (Tracks A+B+C) NOW COMPLETE & LIVE.**
- **BLOCKERS:** (1) Airtable metadata API STILL 403 on Netlify token — Netlify's AIRTABLE_TOKEN lacks schema scope (not the token owner edited); need schema-write token (clipboard) or manual add for ~5 new fields. (2) Repo visibility unknown — confirm PRIVATE before committing OTT's confidential price/cal data (gh not authed).

**THREE FIXES SHIPPED 2026-07-10 (master @ 0efd15a, deploy ready + live-verified):** (1) **Report-month bug FIXED** — `installer-closeout.js` was stamping `Calibration Date` = the close-out day (`now`), so a June 28 Omaha event closed out July 10 landed on JULY's report, leaving June empty. Now stamps the booking's **`Event Date`** (falls back to `now` only for walk-ins w/ no event); cert "Date Calibrated" follows. **4 existing June records were corrected in Airtable** (2026-07-10 → 2026-06-28) via a one-off script (since deleted) → June report now correctly shows 4 calibrations ($260 + 3 VFT "9.2 New" rows needing commission confirm). (2) **Online review-then-send** — new token-gated `ott-report-review.js`: renders the month's report in-browser + **Download Excel (.xlsx)** button + **Send to OTT** button; the draft + reminder emails now link HERE (was linking straight to the send endpoint). `subTable` exported from `lib/ott-report.js`. (3) **Deadline reminder** — new scheduled `ott-report-reminder.js` fires the **5th** (`0 13 5 * *`, email+Slack, OWNER ONLY, never OTT) if the prior month still has calibrations; **submission deadline moved 10th→7th** (owner's new cadence), SOP updated. 407 tests green. Live-verified: review page 200 (shows rows, both buttons) + xlsx download 200 (correct MIME, 6.6KB). NOTE: OTT "Vehicle Year" still derives from the Vehicle text RANGE (shows range-start, e.g. 2016 not the real model year) — the report does NOT yet use the captured `Model Year` field; candidate follow-up now that model-year capture is live (see [[booking-model-year-capture]]).

**REVIEW CONSOLE SHIPPED 2026-07-10 (master @ b7b91e2, deploy ready + live-verified):** `ott-report-review.js` is now the owner's always-on **OTT Commission Report** dashboard (token-gated, bookmarkable, `?month=` switch, defaults to prior month). **① Completed = the submission**: commission auto-fills from the price sheet AND is **editable per row**; Save (POST → Airtable) persists to a **`Commission Override`** column that wins over the auto amount everywhere (download, send, monthly draft) — honors a real $0, blank clears it. `buildSubmissionRows` prefers the override. **② Overdue/incomplete list** (`buildOpenBookings`): past events (Event Date < today) not Completed/Cancelled/No-show, grouped by installer via `keyToInstaller` — the chase list, never submitted. **⚠ OWNER MUST ADD one Airtable column: `Commission Override` (Number) on Bookings** — metadata API still 403s (token lacks schema scope, confirmed again 2026-07-10), so it's a manual add; until then Save returns `{ok:false,error:"missing-column"}` and the page tells the owner to add it (feature is fully tolerant). 415 tests green. Owner supplied the full **`Apr 2026 - OTT_PriceSheet - Protected.xlsx`** (21 tabs: 17 platform price tabs w/ an "OTT Commission" col + Definitions/Notes/Costs) — confirmed it's the same sheet already encoded in `lib/ott-commission-template.json`; unresolvable cal types (VFT "9.2 New", bench BB, superchargers) are exactly what the manual override is for. Read a protected multi-sheet .xlsx via a raw ZIP/inflate dump (each sheet's real title is in its row 0; the workbook.xml tab order does NOT match sheetN.xml filename order).

**POLICY-0012 COMPLIANCE + OWNER-EDIT CONSOLE 2026-07-10 (master @ da4f74e, live-verified):** After reading all 13 OTT policy memos (see [[ott-policy-standards]]), aligned the submission to **Policy 0012**: (1) added the required **15th "Notes" column** (after Commission; sourced from optional `Bookings.Notes`); (2) **Commission formatted "$X.00"/"$0.00"** in the .xlsx (row objects stay numeric for the console); (3) **Gear Size** normalized to 2 decimals; (4) **Engine Size** 2.4L trucks → **2.4T/2.4TH**; (5) **Vehicle Type** added **"RAV 4"** (space!), ES350, LS460. Also: **OTT Retailer col C = "Tuned Yota - Aaron/Cody/Noah"** per booking installer (`retailerFor`); **GRAND TOTAL** of Commission in col N, 2 rows below last record ("$X.00"). **RESOLVED 2026-07-10:** (a) **grand-total row REMOVED** from the submitted workbook (master @ 4d1bc69) to comply with 0012's "no additional formatting / any deviation breaks reporting"; the running commission total stays visible on the review console instead. (c) **header text: owner said KEEP OURS** ("Date of Submission" / "Customer First Last Name") — no change. ⚠ **STILL OPEN FOLLOW-UPS:** (b) RX350 not in 0012's Vehicle Type list — how to report?; (d) **Policy 0008 pricing-adherence deviation report** (our retail vs OTT Suggested Price) — build on request (our retail deviations are OTT-approved, do NOT change pricing). Commission mapping rule (owner-confirmed): **VFT & PCM default to Base; MAF Scale / Custom are explicit upgrades**; "9.2 New" on a 3rd Gen Tacoma 3.5L VFT = VFTuner Base = **$110** (cells I9=2.7L, I3=3.5L). **"9.2 Update" is a FREE service → reports as $0.00** (owner, 2026-07-11). So Calibration Type "9.2 Update" → $0 regardless; "9.2 New" (3rd Gen Tacoma 3.5L VFT) → $110. **4th Gen Tacoma (2024+, 2.4T gas / 2.4TH turbo-hybrid, VFT — never COBB): commission defaults to Stage 1 = $160, with a console pick-list for Stage 1 Custom = $250 and Stage 3 Custom = $350** (`commissionCandidates()` in ott-commission.js; master @ 8ab659d). **BUG FIXED 2026-07-11 (ce9e36e): `engineSize` was non-idempotent** — deriveVehicle emitted "2.4T" but engineSize("2.4T") re-normalized to "2.4", so the 4th Gen Tacoma commission lookup silently returned nothing (CONFIRM). Now any 2.4L form (2.4/2.4L-T/2.4T/2.4TH) maps idempotently → resolves to $160. Regression test added. Mallory Sampson **duplicate booking DELETED** 2026-07-11 (was recgdwH1TCSLKVxU4). June's 3 VFT "9.2 New" (Louis + 2× Mallory) set to $110 via override; June total now $590 (Connor $260 auto + 3×$110). **Owner-edit console** (`ott-report-review.js`): the Overdue/Incomplete section is now editable — owner completes ANY installer's booking inline (POST `op:complete`, no ownership check, Calibration Date=event day) + an **"Add a walk-in"** form (POST `op:walkin`, creates a completed record, `Source:"owner:walk-in"`); both use `reportFields()` + create/updateTolerant. Walk-in path verified live (create→persist→delete). **Deviation report (Policy 0008 pricing adherence) PAUSED as a follow-up** — owner's retail-price adaptations are OTT-approved, DO NOT change pricing (commission still comes from the sheet). Flex-fuel Tundra note (0011) added to installer itinerary — see [[event-reminders-automation]].

**JUNE RECONCILED + REPORT ENHANCEMENTS 2026-07-11 (master @ c52d888):** Loaded the owner's full June calibration list (28 rows) into Airtable via a one-off matcher (matched by VIN → UPDATE existing / CREATE walk-in `Source:"owner:reconcile"`, `Certificate Sent`=true to avoid cert-hold spam since these have no customer tier). Result: **June = 26 completed calibrations, ~$2,790, only 3 needing commission**. Mallory's duplicate booking was set `Status:Cancelled` (removed double-count). 2 July-dated rows correctly land in July's report. Report enhancements now LIVE: (1) **ECU ID auto-fills** from model+year when blank (3.5L 3rd Gen Tacoma map in `lib/ecu-ids.js`, most-likely Auto first); installer/loaded value wins. (2) **Gear default** (owner rule): auto 3rd Gen Tacoma → 3.90, everything else (incl. manual 3rd gen + 2.7L 3rd gen + non-Tacomas) → 4.30; gear rule covers ALL 3rd gen Tacomas (2.7+3.5). (3) **Report gate relaxed**: a completed booking is reportable with a Calibration Type even WITHOUT the internal customer tier (`OTT Calibration`). (4) **Two-section workbook**: 3rd Gen Tacoma **3.5L ONLY** (owner clarified 2026-07-11) sorted by Date Calibration Applied → 2 blank rows → all other vehicles. NOTE: `is3rdGenTacoma35` (3.5-only) drives the SECTION; `is3rdGenTacoma` (broad, 2.7+3.5) drives the gear default. Airtable has NO `Notes` field, so the report's Notes column (col 15) is always blank until the owner adds one. STILL TODO: **ECU/gear dropdown+override UI on the console completed rows** (auto-fill shipped; interactive picker pending); the 3 unresolved commissions; RX350 vehicle type; pricing-adherence report.

**✅ JUNE 2026 SUBMITTED TO OTT 2026-07-11** (owner approved after reviewing the downloaded .xlsx → said "send it"): 35 calibrations · **$4,425** commission · emailed to info@overlandtailor.com + hgobbels@me.com (CC info@) via `ott-report-send.js`, HTTP 200 "Submission sent ✓". Date of Submission stamped 2026-07-11. First real end-to-end OTT submission through the full review→approve→send pipeline.

**⚑ STANDING RULE — owner reviews the actual downloaded .xlsx as the FINAL approval gate before any OTT send (owner reinforced 2026-07-11).** Never trigger Finalize & Send (`ott-report-send.js`) autonomously; deliver the workbook + summary, then WAIT for an explicit "send it" each month. Sending emails an external commission doc to info@overlandtailor.com + hgobbels@me.com — treat as irreversible outward action. This is rule #1 made concrete: the download-and-eyeball step IS the approval.

**WORKBOOK FINISHING TOUCHES 2026-07-11 (master @ c2bd3bd, live-verified):** three owner-requested polish items on the submitted .xlsx: (1) **column widths** — `xlsx-writer.js` gained optional `<cols>` support; `renderOttXlsx` sets `COL_WIDTHS` (15 legible widths). (2) **Grand Total REINSTATED** — sum of Commission in **column N**, labelled "Grand Total" in col M, **two rows below the final entry** (one blank spacer + total row). NOTE: this REVERSES the earlier 4d1bc69 removal — owner now explicitly WANTS the grand total (the Policy-0012 "no extra formatting" concern is overridden by direct owner instruction). (3) **Date of Submission stamped = today** on the review-console download too (`loadMonth` passes `sendDate`), so a downloaded workbook is submission-ready (caveat: it stamps the DOWNLOAD/generation day — re-download the day you actually send if different). Rewrote the stale "no grand-total row" test → asserts placement; added a column-width test. 450 tests green.

**NOTES COLUMN EDITABLE ON CONSOLE 2026-07-11 (master @ 89a0578, live-verified):** the Policy-0012 15th column (**Notes**) is now enterable end-to-end on `ott-report-review.js` — an editable Notes input on every completed row (placeholder "MAF scale, SC type, bench…") + a Notes field in the overdue close-out and walk-in editors. `saveOverrides` persists `Notes`, and does so **tolerantly** (`updateTolerant(..., ["Notes"])`) so a not-yet-added column drops gracefully instead of failing the whole save (unlike `Commission Override`, whose absence is still surfaced). `reportFields()` + `OPT_FIELDS` carry Notes for complete/walk-in. Data layer already emitted `b.Notes` into the .xlsx (col 15). **⚠ OWNER ACTION: add a `Notes` field (long text) to Airtable Bookings** — until then typed notes save-succeed but are silently dropped (metadata API still 403s → manual add, same as VIN/Commission Override). Brett Dunn's $0 (CUW/Stock tune) confirmed CORRECT by owner, left as-is. 449 tests green. Preview technique: render `reviewPageHtml` with a REDACTED token + mock bookings to a static file for screenshots (never screenshot the live URL — token is a query param).

**SOP set WRITTEN 2026-07-02:** `docs/operations/` now holds a 10-SOP operations manual (README + client-marketing, lead-tracking, event-booking, event-closeout, priority-waitlist, installer-field-guide, event-scheduling, monitoring-incident-response, monthly-ott-report, data-security-secrets) grounded in the live functions. Uncommitted at time of writing unless owner shipped. Future **per-vehicle OTT installation guide** (brand/model/year/engine) — "coming days," not started.
