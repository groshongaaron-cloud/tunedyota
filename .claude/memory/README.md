# Project memory (mirror)

Snapshot of Claude Code's persistent project memory, mirrored here for version control /
backup. The **live** memory lives at `~/.claude/projects/<this-project>/memory/` and keeps
updating there — this copy is a point-in-time mirror and may drift. Re-copy to refresh.
